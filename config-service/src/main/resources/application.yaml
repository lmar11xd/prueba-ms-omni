server:
  port: 6060
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/lmar11xd/prueba-ms-omni
          search-paths: config-data
application:
  name: config-service